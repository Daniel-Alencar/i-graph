// R: Números reais
// I: Números complexos

export function operation_R1_I(x) {
  const result = math.pow(-1, x);
  return result;
}

export function operation_R2_R(x, y) {
  const result = math.pow(-1, x + y);
  return result;
}
